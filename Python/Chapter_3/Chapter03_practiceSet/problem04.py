name = "Yogendra   is a  good Boy"

print(name.replace("  "," ")) # if output is -1 so in your string no double white space
print(name.replace("   "," Patidar "))
print(name)  # strings are immutable which means that you cannot change them  by running function on them

